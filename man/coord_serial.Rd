% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord_serial.R
\name{coord_serial}
\alias{coord_serial}
\title{Serial Coordinate System}
\usage{
coord_serial(
  domain_gap = 0.01,
  domain_order = NULL,
  scaffold = NULL,
  ylim = NULL,
  expand = TRUE,
  clip = "on"
)
}
\arguments{
\item{domain_gap}{Fractional gap between domains as a proportion of total
length. Default \code{0.01} (1\%).}

\item{domain_order}{Vector specifying the order of domains. If \code{NULL}
(the default), domains are sorted naturally.}

\item{scaffold}{Optional named vector of domain lengths. If provided, these
lengths are used instead of calculating them from the data. This allows
showing empty regions or missing domains.}

\item{ylim}{Numeric vector of length 2 giving y-axis limits, or \code{NULL}
(default) for automatic limits.}

\item{expand}{If \code{TRUE} (the default), adds a small expansion factor to the
axes.}

\item{clip}{Should drawing be clipped to the extent of the plot panel?
\code{"on"} (the default) or \code{"off"}.}
}
\value{
A ggplot2 \code{Coord} object that can be added to a ggplot.
}
\description{
\code{coord_serial()} provides a coordinate system for plotting data along a
continuous axis composed of multiple discrete domains. It takes
per-domain positions and maps them to a continuous serial x-axis with
domain labels — similar to a manhattan plot — without requiring manual
data preprocessing.
}
\details{
The domain grouping is specified via the \code{domain} aesthetic in \code{aes()}.
Internally, \code{coord_serial()} transforms each layer's x-positions into
cumulative coordinates and relabels the x-axis with domain names.
}
\examples{
library(ggplot2)

# Generate genetics-specific data on the fly
simulated_gwas <- simulate_gwas()

ggplot(simulated_gwas, aes(x = position, y = log10p, domain = chrom)) +
  geom_point(size = 0.5) +
  coord_serial()

# Use a scaffold to show all chromosomes even if some are missing data
hg38 <- c("1" = 248956422, "2" = 242193529, "X" = 156040895)
ggplot(simulated_gwas, aes(x = position, y = log10p, domain = chrom)) +
  geom_point(size = 0.5) +
  coord_serial(scaffold = hg38)

}
